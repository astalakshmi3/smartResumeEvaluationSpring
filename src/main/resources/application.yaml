spring:
  application:
    name: smartResumeEvaluation

  ai:
    retry:
      max-attempts: 1

    openai:
      api-key: ${TEST}
      # base-url: https://api.openai.com/
      chat:
        options:
          model: gpt-4o-mini
          max-tokens: 1500
          temperature: 0.7