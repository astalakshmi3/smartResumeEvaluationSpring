spring:
  application:
    name: smartResumeevaluation

  ai:
    model:
      chat: openai
    retry:
      max-attempts: 1
  openai:
      api-key: ${KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
          max-tokens: 1500